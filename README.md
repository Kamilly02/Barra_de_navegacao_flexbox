# Projeto 04 (Trilha Simplificada) – Cabeçalho com Flexbox

## 📌 Sobre o Projeto

Este projeto aprofunda ainda mais o uso do **Flexbox**, agora aplicado a um componente extremamente comum em qualquer site: o **cabeçalho (header)**.

Aqui, vamos construir um header simples e funcional, contendo um **logotipo alinhado à esquerda** e um **menu de navegação alinhado à direita**. O foco está em dominar propriedades importantes como `justify-content` para distribuir espaço entre os itens, além de adicionar interatividade visual com a pseudo-classe `:hover` nos links.

Este é um componente que você irá criar inúmeras vezes ao longo da carreira — entender seu funcionamento é essencial.

---

## 🛠️ Tecnologias e Conceitos Abordados

### **HTML5**

* Estruturação do cabeçalho usando `<header>`.
* Uso de listas `<ul>` e `<li>` para navegação.
* Uso adequado da tag `<a>` para links.

### **CSS3 – Flexbox**

* Criação de um contêiner com `display: flex`.
* Uso de `justify-content: space-between` para separar logo e navegação.
* Alinhamento vertical com `align-items: center`.
* Estilização dos links com `:hover` para dar feedback ao usuário.

---

## 📂 Estrutura de Arquivos Sugerida

```
projeto-04-header-flexbox/
├── assets/
│   └── logo.png
├── index.html
└── style.css
```

---

## 🎯 Objetivo Principal

Compreender como:

* Criar e posicionar elementos dentro de um cabeçalho.
* Utilizar o Flexbox para distribuir e alinhar itens.
* Estruturar menus de navegação limpos e organizados.
* Aplicar efeitos simples com `:hover` para melhorar a experiência do usuário.

---

## 🧪 Resultado Esperado

Um cabeçalho contendo:

* Um logotipo à esquerda.
* Uma lista de links alinhados à direita.
* Layout limpo, equilibrado e funcional.
* Interação visual suave ao passar o mouse sobre os links.

Este projeto prepara você para construir layouts completos, barras de navegação profissionais e estruturas comuns em sites modernos.


